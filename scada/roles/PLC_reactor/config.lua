return {
  id = "reactor-1",
  facility = "giggle-plant-alpha",

  modem_side = "back",

  bundled_input = {
    side = "top",
    fuel     = colors.purple,
    temp     = colors.orange,
    waste    = colors.brown,
    critical = colors.black,
  },

  bundled_output = {
    side = "left",
    power = colors.red
  }
}